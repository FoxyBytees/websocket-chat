{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"command": "cargo build --bin client --package client",
			"problemMatcher": [
				"$rustc"
			],
			"group": "build",
			"label": "rust: cargo build",
			"options": {
                "cwd": "${workspaceFolder}\\client",
            }
		}
	]
}